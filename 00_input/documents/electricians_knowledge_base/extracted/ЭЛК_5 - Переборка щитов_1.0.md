# ЭЛК_5 - Переборка щитов_1.0

Source document: `doc_017`

Source file: `00_input/documents/electricians_knowledge_base/raw/ЭЛК_5 - Переборка щитов_1.0.docx`

Source format: `DOCX`

Extraction mode: controlled page-aware assessment.

Important extraction note:

- This file is not a normal prose extraction.
- The DOCX contains visual wiring schemes assembled from many embedded image fragments plus short labels.
- The valuable schemes are preserved as separate DOCX-derived visual review artifacts: `img_0123` through `img_0129`.
- No source-backed atomic statements were promoted from this document in batch 017.
- Primary scheme descriptions are tracked in `00_input/documents/electricians_knowledge_base/statements/review/doc_017_visual_schemes_review.md`.

## Page 1

Visual context: component lineup.

Visible labels include `A1` and `A2` on the contactor module.

Review artifact: `img_0123` / `doc017_scheme_001_page_1_component_lineup.png`.

Extraction decision: preserved for manual review. The page shows component context but does not provide a safe standalone source-backed instruction before expert review.

## Page 2 Left Scheme

Visible labels include `Ввод`, `N`, and contactor coil labels `A1` / `A2`.

Review artifact: `img_0124` / `doc017_scheme_002_page_2_left.png`.

Extraction decision: preserved for manual review. The wiring relation is carried by visual line routing, not by a text-backed sentence.

## Page 2 Right Scheme

Visible labels include `Ввод`, `N`, `Фаза на инвертор "in"`, `Фаза от инвертора "OUT"`, and contactor coil labels `A1` / `A2`.

Review artifact: `img_0125` / `doc017_scheme_003_page_2_right.png`.

Extraction decision: preserved for manual review. The page has meaningful schematic labels, but the exact conductor routes and terminal assignments require expert visual review.

## Page 3

Visible labels include `Ввод`, `N`, `Фаза на инвертор "in"`, and `Фаза от инвертора "OUT"`.

Review artifact: `img_0126` / `doc017_scheme_004_page_3_assembled.png`.

Extraction decision: preserved for manual review. The source provides a single assembled scheme but no explanatory text that can be safely promoted into canonical statements before expert review.

## Page 4

Visible labels include numbered outputs `1` through `6` and a lower legend with `PE`, `L вход input`, `N вход input`, `L выход output`, `N выход output`.

Review artifact: `img_0127` / `doc017_scheme_005_page_4_outputs_1_6.png`.

Extraction decision: preserved for manual review. Numbered output mappings are diagram-only and must not be converted into statements without manual source review.

## Page 5

Visible labels include numbered outputs `1` through `6` and the lower `PE / L / N / input / output` legend.

Review artifact: `img_0128` / `doc017_scheme_006_page_5_split_outputs.png`.

Extraction decision: preserved for manual review. The scheme appears to be an alternate routing variant and requires expert review before any source-backed statement is written.

## Page 6

Visible labels include numbered outputs `1` through `6` and the lower `PE / L / N / input / output` legend.

Review artifact: `img_0129` / `doc017_scheme_007_page_6_alternate_routing.png`.

Extraction decision: preserved for manual review. The scheme is visual-first and has no safe text-backed atomic statements for automatic canonical promotion before expert review.
