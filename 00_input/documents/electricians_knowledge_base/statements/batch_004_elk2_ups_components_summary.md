# Batch 004: ЭЛК 2 / состав ИБП

Дата: 2026-06-17

Модельный режим: `5.5`, высокий уровень.

## Источник

```text
ЭЛК_2_Базовые_знания_Состав_ИБП_ред1_5.docx
```

## Результат

- обработано `24` чанка;
- добавлено `59` atomic statements;
- canonical расширен до `224` валидных утверждений;
- создан кластер `C007 / ups_components`;
- создан редакционный раздел `02_ups_components.md`;
- добавлено `35` связей `statement_id -> image_id`;
- `14` изображений скопированы в редакционный слой и normalized-слой;
- decorative/icon image `img_0041` не привязано как знание.

## Особые решения

Опасные технические темы помечены как `safety_critical` и `review_required`:

- байпас и переключение питания;
- работа байпаса в разных режимах;
- группа АКБ;
- балансиры и схемы подключения;
- выравнивающие перемычки;
- DC-защита;
- подключение GSM-розетки вне резервной группы.

Проблемы источника вынесены в `source_quality_issues.md`:

- опечатка `ИПБ` вместо `ИБП`;
- оборванная фраза про приложение Телеметрика.

## Проверки

- `validate_atomic_statements.py` для batch 004: passed, `59` statements.
- `validate_atomic_statements.py` для canonical: passed, `224` statements.
- `check_source_coverage.py` для 7 редакционных источников: passed, `0` uncovered content chunks.
